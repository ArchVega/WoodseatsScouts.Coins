CreateTodoReport may not be capturing all todos. Sense check by doing a string token search for all "todos"; regex matches should equal this number.
Write acceptance test for "member code in coin" and vice versa
What happens if someone cancels a haul? The coins are already assigned on scan.
"User not found image" is not displaying when no image is found for a member.
The "enormous scavenger haul" test isn't completing sometimes. 
Found a code B00100 (coin, 0 value?) might be nothing as its in the scavengedcoins table (test data, GetTopThreeGroupsInLastHour_FourScavengeResults_ResultsShouldBe3)
Clear coin code when scanned
DB - Sections.Code = Members.SectionId. Rename Members.SectionId to SectionCode
Write test - "two users scan the same coin, first user finishes and records the coin, second user finishes and receives an error (second user has unscavenged coins recorded and error coins logged"
need to track scanned-coins-for-current-user so that they don't scan a coin twice! total coins screen needs to be adjusted accordingly