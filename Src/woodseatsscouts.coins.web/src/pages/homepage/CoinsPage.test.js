import { render, screen } from '@testing-library/react';
import CoinsPage from "./CoinsPage";

test('home page', () => {
    // render(<CoinsPage/>)
    //
    // const homePageElement = screen.getByText(/Something/i);
    //
    // expect(homePageElement).toBeInTheDocument();
    // expect(homePageElement).toHaveClass('classname');
})